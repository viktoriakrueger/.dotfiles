# osx

font:
  # Point size
  size: 12.0

window:

  dimensions:
    columns: 205
    lines: 55

  decorations: buttonless

shell:
  program: /bin/zsh
  args:
   - --login
   - -c
   - "tmux attach || tmux ; pkill spotifyd"

import:
  - ~/.dotfiles/alacritty/alacritty/alacritty.yml
