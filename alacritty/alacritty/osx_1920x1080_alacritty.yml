# osx

font:
  # Point size
  size: 13.0

window:

  dimensions:
    columns: 239
    lines: 57

  decorations: buttonless

shell:
  program: /bin/zsh
  args:
   - --login
   - -c
   - "tmux attach || tmux ; pkill spotifyd"

import:
  - ~/.dotfiles/alacritty/alacritty/alacritty.yml
