# osx

font:
  # Point size
  size: 13.0

window:
  decorations: buttonless

shell:
  program: /bin/zsh
  args:
   - --login
   - -c
   - "tmux attach || tmux"

import:
  - ~/.dotfiles/alacritty/alacritty/alacritty.yml
