# osx

font:
  # Point size
  size: 17.0

window:

  dimensions:
    columns: 255
    lines: 65

  decorations: buttonless

shell:
  program: /bin/zsh
  args:
   - --login
   - -c
   - "tmux attach || tmux ; pkill spotifyd ; pkill spt"

import:
  - ~/.dotfiles/alacritty/alacritty/alacritty.yml
