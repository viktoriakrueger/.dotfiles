# osx

font:
  # Point size
  size: 14.0

window:

  dimensions:
    columns: 250
    lines: 60

  decorations: buttonless

shell:
  program: /bin/zsh
  args:
   - --login
   - -c
   - "tmux attach || tmux ; pkill spotifyd"

import:
  - ~/.dotfiles/alacritty/alacritty/alacritty.yml
