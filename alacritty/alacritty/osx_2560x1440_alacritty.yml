# osx

font:
  # Point size
  size: 15.0

window:

  dimensions:
    columns: 278
    lines: 68

  decorations: buttonless

shell:
  program: /bin/zsh
  args:
   - --login
   - -c
   - "tmux attach || tmux ; pkill spotifyd"

import:
  - ~/.dotfiles/alacritty/alacritty/alacritty.yml
